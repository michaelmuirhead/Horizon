// Legacy route. Editing now lives at /planner/{folderId}/{budgetId}/{entryId}.
// Kept as a stub so old bookmarks don't 500; renders nothing.
export default function LegacyPlannerEntryPage() {
  return null;
}
