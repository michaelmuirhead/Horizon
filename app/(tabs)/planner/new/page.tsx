// Legacy route. Adding now lives at /planner/{folderId}/{budgetId}/new.
// Kept as a stub so old bookmarks don't 500; renders nothing.
export default function LegacyPlannerNewPage() {
  return null;
}
